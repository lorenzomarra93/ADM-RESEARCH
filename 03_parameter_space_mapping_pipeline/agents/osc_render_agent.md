# OSC Render Agent

Handles OSC/ADM-OSC message generation and logging.
- Use `send_adm_osc.py` and `sync_transport.py`.
- Archive logs under `outputs/osc_logs/` and renders under `outputs/test_renders/`.
