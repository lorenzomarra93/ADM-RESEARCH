# Controller Policy

1. Register each controller (hardware/software) with capabilities + limitations.
2. Map controller data ranges to normalized values before routing to OSC.
3. Log calibration sessions in `data/control_data/`.
4. Store fallback strategies for controller failure.
